# Laravel backend base URL (no trailing slash). API routes are under /api.
# Dev: Laravel backend URL (no trailing slash). Leave empty for production (uses same-origin /api).
VITE_API_URL=http://localhost:8000/api
